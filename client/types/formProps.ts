export type ProductData = {
  name: string;
  description: string;
  price: number;
  code: string;
  checkout: string;
};
export type formDataProp = {
  name: string;
  description: string;
  isDisabled: boolean;
};
export type ImageData = {
  imageURL: string;
  isUploading: boolean;
};
